#include "main.h"
/*
 * main - Entry point
 *
 * Return: 0 Always (Succces)
 */
void print_alphabet(void)
{
	char alpha;

	for(alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar('a');
	}
	_putchar('\n');
	return ();
}
