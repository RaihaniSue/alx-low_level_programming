#include "main.h"
#include <stdio.h>
/**
 * swap_int - Entry point
 * @a: first parameter
 * @b: second parameter
 * Return: 0 Always for success
 */
void swap_int(int *a, int *b)
{
	int x;

	x = *a;
	*a = *b;
	*b = c;
}
