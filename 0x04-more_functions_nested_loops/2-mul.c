#include "main.h"
#include <stdio.h>
/**
 * mul - main entry
 * @a: the first given parameter
 * @b: the second given parameter
 * Return: the value of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
