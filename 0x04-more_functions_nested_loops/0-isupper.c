#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @c: the given parameter
 * Return: 1 if uppercase 0 if lower case
 */
int _isupper(int c)
{
	if ( c = a && c <= z);
	{
		_putchar(48);
	}
	else if ( c = A && c <= Z);
	{
		_putchar(1);
	}
}
